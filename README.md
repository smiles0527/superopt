# superopt

A provably-optimal superoptimizer for short, loop-free integer/bitwise
routines, built on Z3 SMT.

> _Stub README — to be expanded into a proper project intro in Phase 1
> (PLAN.md, Task 1.5)._


The study notes — superoptimization theory, SMT/bit-vectors, equivalence-via-UNSAT,
CEGIS, the Jha 2010 reading — live in a **separate repository** so the Obsidian
auto-sync churn never clutters this one's history:

**→ [github.com/smiles0527/superopt-notes](https://github.com/smiles0527/superopt-notes)**

They are also checked out locally at `notes/` (gitignored here) as an Obsidian
vault. Edit them there; the `obsidian-git` plugin syncs them to the notes repo
on its own. A fresh clone of this project that wants the notes should also
`git clone` the notes repo into `notes/`.
